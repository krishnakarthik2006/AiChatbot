# ai_chatbot source documents

Put approved source documents in this folder (PDF, Markdown, HTML, or text). Run `POST /api/documents/ingest` after changing a document.

No sample policy facts are included here: upload official material before exposing the service to users.
